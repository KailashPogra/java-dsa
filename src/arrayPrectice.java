import java.util.Scanner;
public class arrayPrectice {
    public static void main(String[] args) {
        // calculate the sum of all given array
      /*  int[] arr = {1,5,3,5,5,5,6,7,8};
        int sum = 0;
        for (int i = 0; i < arr.length ; i++) {
            sum+=arr[i];
        }
        System.out.println(sum); */
       // calculate the maximum value out off all the element in the array
      /* int array[] = {1,5,3,8,9,12};
        // required to be output is 12
        int ans = 0;
        for (int i = 0; i < array.length ; i++) {
              if (array[i]>ans){
                  ans=array[i];
              }
        }
        System.out.println(ans);*/
      // search the given element  x the array if present then return index else return -1;
     /*
        // it is a linear search
        int arrays[] = {1,5,3,2,5,3};
        Scanner sc = new Scanner(System.in);
        System.out.println("enter number");
        int x = sc.nextInt();
          int ans = -1;
        for (int i = 0; i < arrays.length; i++) {
            if (arrays[i]==x){
               ans = i ;
               break;
            }
        }
        System.out.println(" found " +x+ " at index "+ ans);
       */

        
    }
}
